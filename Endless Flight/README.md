# How to Run

1. Install the server (First time only): Open your terminal and run `npm install -g http-server`
2. Navigate to the project folder: Use the command `cd TheAviator`
3. Start the server: Run `http-server` and open `http://127.0.0.1:8080` or `http://192.168.12.2:8080` in your browser